## Creating Project in Django using the CRUD (Create, Retrieve, Update, Delete) operations.
### We'll be creating project that will consist of :
creating an app
creating the model
creating view
defining URLs
creating templates
